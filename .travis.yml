language: python
python:
  - "2.6"
  - "2.7"
  - "3.2"
  - "3.3"
  - "3.4"
# command to install dependencies
install: "pip install -r requirements/requirements-devel.txt"
# command to run tests
script: nosetests --with-coverage --cover-package=salteval
